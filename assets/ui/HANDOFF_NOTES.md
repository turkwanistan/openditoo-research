# Handoff Notes

## User-approved baseline

The current approved direction is:

- layout from `assets/baseline_layout`
- activity behavior from `assets/blue_override_variant`

The user explicitly liked the blue-override behavior.

## Important preservation constraints

- Keep the 16×16 geometry exactly consistent with the approved mockups.
- Keep letters and icons separate.
- Keep the activity animation at the very top.
- Keep 1 blank divider row between letters and icons.
- Keep the exact 5×5 skull source shape; only the skull eyes change state color.
- Keep bunny eye recolor behavior.
- Keep mushroom cap recolor behavior.
- Keep RGB222 compatibility.

## Most relevant preview files

Start with these:

- `assets/baseline_layout/MCP_PAGE_LAYOUT_TWEAKS.png`
- `assets/blue_override_variant/BLUE_OVERRIDE_COMPARISON.png`
- `assets/blue_override_variant/all_three_blue_override_activity.gif`

